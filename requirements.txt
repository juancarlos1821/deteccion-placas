# Dependencias de produccion. La inferencia corre sobre ONNX Runtime, asi que
# aqui NO estan PyTorch (468 MB), PaddlePaddle (299 MB) ni sus arrastres.
#
# Los modelos son los mismos: se exportaron a ONNX y se verifico que dan el
# mismo resultado (ver comparar_motores.py y comparar_deteccion.py).
# Medido: 236 MB de RAM en el pico, frente a los 512 MB que da Render gratis.
#
# Para desarrollar en local con el video en directo hace falta ademas
# `pip install ultralytics torch` — la app lo detecta y lo activa sola.

# --- Web ---------------------------------------------------------------
Flask==3.1.2
Werkzeug==3.1.4
python-dotenv==1.2.2
gunicorn==23.0.0

# --- Base de datos -----------------------------------------------------
psycopg2-binary==2.9.12

# --- Vision e inferencia -----------------------------------------------
# Solo la variante headless de OpenCV: en un servidor no hay escritorio.
opencv-python-headless==4.6.0.66
numpy==1.26.4
onnxruntime==1.29.0
